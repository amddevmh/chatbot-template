/**
 * Re-export the useAuth hook from the auth-provider component
 * 
 * This file exists to maintain the same import path for components that use the hook.
 */
export { useAuth } from '../components/auth-provider';
